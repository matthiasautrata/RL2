# Future Reference Implementation

This directory contains implementation-oriented design retained for a follow-on evaluator project.
It is informative and does not prescribe a conforming RL2 implementation.

The canonical RDF-to-AST mapping and pure evaluation equivalence obligations are language concerns;
they will be extracted into `../../spec/`. Indexes, effects, commits, caching, and concrete IR
layout remain implementation choices.
